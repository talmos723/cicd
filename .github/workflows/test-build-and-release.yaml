name: Test, build and release
on: [push]
jobs:
  unit-test:
    name: Unit test
    runs-on: ubuntu-latest
    steps:
      - name: Install check and pkg-config
        run: |
          sudo apt install check pkg-config
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Building with check
        run: |
          gcc operations.c check_operations.c -o check_operations \
          `pkg-config --cflags --libs check`
      - name: Testing with check
        run: |
          ./check_operations

  splint-test:
    name: Splint test
    runs-on: ubuntu-latest
    steps:
      - name: Install splindt
        run: |
          sudo apt install splint
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Running lindt
        run: |
          splint --compdef --exportlocal simplecalculator.c operations.c

  building-and-delivery:
    name: Build and delivery
    permissions:
      contents: write
    needs: [unit-test, splint-test]
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Neptun code insert
        run: |
          echo "ala54q" > neptun.txt
      - name: Building
        run: |
          gcc operations.c simplecalculator.c -o simplecalculator
      - name: Creating tar.gz
        run: |
          tar -czvf simplecalculator.tar.gz simplecalculator neptun.txt
      - uses: "marvinpinto/action-automatic-releases@latest"
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: "latest"
          title: "Development build"
          files: |
            simplecalculator.tar.gz
